// Tournament Intel's offline fallback — used when the API is unreachable,
// same pattern as every other mode's fallback bank.
export const BRIEFING_FALLBACK = "Gate 4 remains the day's primary pressure point at 97% capacity, with Concourse S rerouting holding it steady. Ice Cream Co.'s stock is the one vendor risk worth a restock call before it runs out. Recycling is tracking on target. Recommended action: pre-position an extra marshal at Gate 4 ahead of full-time.";
